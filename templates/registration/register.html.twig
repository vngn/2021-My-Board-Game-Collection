{% extends 'base.html.twig' %}

{% block titre %}
{{ parent() }} | Enregistrement
{% endblock titre %}

{% block bg %}
{{ include('includes/bg-visiteur.html.twig') }}
{% endblock bg %}

{% block body %}

<section class="page-form page-first">

    {% for flashError in app.flashes('verify_email_error') %}
    <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <h1>Enregistrement</h1>

    {{ form_start(registrationForm) }}
    <div class="row mt-5">
        <div class="col-12">
            {{ form_row(registrationForm.pseudo, {label: 'Pseudo'}) }}
            {{ form_row(registrationForm.email, {label: 'Email'}) }}
            {{ form_row(registrationForm.plainPassword) }}
        </div>

        <p>En renseignant vos informations personnelles, vous acceptez d'être contacté(e) pour la gestion de votre
            compte et
            vous acceptez les termes de notre <a href="{{ path('legalNotice') }}"><span class="link">politique de
                    confidentialité<span></a></p>
        <p>Vous pouvez retirer votre consentement à tout moment depuis le <a href="{{ path('contact') }}"><span
                    class="link">formulaire de contact<span></a> ou en nous contactant à l’adresse suivante :
            vngn@laposte.net</p>
        {{ form_row(registrationForm.agreeTerms, {label: 'Accepter les conditions'}) }}

        <div class="col-1">
            <button type="submit" class="btn btn-primary">
                Valider
            </button>
        </div>
    </div>
    {{ form_end(registrationForm) }}
</section>

{% endblock body %}